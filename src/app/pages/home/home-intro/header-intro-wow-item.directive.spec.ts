import { HeaderIntroWowItemDirective } from './header-intro-wow-item.directive';

describe('HeaderIntroWowItemDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderIntroWowItemDirective();
    expect(directive).toBeTruthy();
  });
});
